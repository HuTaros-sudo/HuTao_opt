[project]
name = "genshin-opt"
version = "0.1.0"
description = "Local Genshin Impact artifact optimizer prototype"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
